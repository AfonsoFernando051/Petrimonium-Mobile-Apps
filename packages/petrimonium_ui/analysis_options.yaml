# Inherits the workspace baseline (../../analysis_options.yaml). Add a rule
# here only if it is genuinely specific to petrimonium_ui — anything the whole
# workspace should follow belongs in the baseline, once.
include: ../../analysis_options.yaml
